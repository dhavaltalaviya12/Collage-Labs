<!DOCTYPE html>
<html>
<head>
    <title>Assign Doctor to Department</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body>
    <div class="card shadow-sm rounded-3 m-4">
        <div class="card-header bg-info text-white d-flex justify-content-between align-items-center">
            <h5 class="mb-0">Assign Doctor to Department</h5>
            <a href="/DoctorDepartment/DoctorDepartmentList" class="btn btn-light btn-sm">
                <i class="bx bx-arrow-back"></i> Back to List
            </a>
        </div>

        <div class="card-body">
            <form method="post" action="/DoctorDepartment/Save">
                <input type="hidden" name="DoctorDepartmentID" value="0" />
                <input type="hidden" name="UserID" value="1" />

                <div class="mb-3">
                    <label class="form-label">Select Doctor</label>
                    <select name="DoctorID" class="form-select" required>
                        <option value="">-- Select Doctor --</option>
                        <!-- These options should be generated dynamically from DB -->
                        <option value="1">Dr. A. Patel</option>
                        <option value="2">Dr. R. Sharma</option>
                        <option value="3">Dr. K. Mehta</option>
                    </select>
                </div>

                <div class="mb-3">
                    <label class="form-label">Select Department</label>
                    <select name="DepartmentID" class="form-select" required>
                        <option value="">-- Select Department --</option>
                        <!-- These options should be generated dynamically from DB -->
                        <option value="1">Cardiology</option>
                        <option value="2">Neurology</option>
                        <option value="3">Orthopedics</option>
                    </select>
                </div>

                <button type="submit" class="btn btn-success">
                    <i class="bx bx-save"></i> Assign
                </button>
            </form>
        </div>
    </div>
</body>
</html>
    